# Stage A1 완료 보고 — 메커니즘 확정 (소스 미수정)

- 브랜치: `task/2019-repursue-impl` (devel − 잘못된 #2019 수정)
- 앵커: #2007 `42065_규제영향분석서_공익신고자 보호법.hwp` (rhwp 6p vs 한글 17p)

## ★ 핵심 발견: deep-class 근본원인 통일

**모든 deep-class 이슈는 "stored vpos ≠ 실제 렌더 extent"라는 단일 근본원인.** 방향만 다름.

### 측정 증거 (42065 pi=7)
- pi=7 = 1×1 RowBreak 표(자리차지) 안에 중첩 1×1 표, 중첩셀 135+문단.
- **stored vpos 최대(max vpos+lh) = 66140 HU = 882px** (압축된 좌표).
- **실제 문단 line_height 합 = 실제 콘텐츠 높이 ≫ 882px** (135+ 문단, 수천 px).
- `height_measurer.rs:1024-1042`가 중첩셀 높이를 `last_seg_end`(=max vpos+lh=882px)로 측정
  → 한 페이지(avail 895px)에 맞다고 판단 → 통째 배치(split 미발동) → 콘텐츠 오버플로/크램.

### 분할 메커니즘 갭
- `TABLE_SPLIT_AVAIL` 계측: pi=2(2×1, 직접셀)는 문단 경계(start_cut=[9],[13])로 **정상 3분할**.
- pi=7(1×1)은 split 루프 진입하나 **단일 fragment 통째 배치** — 콘텐츠가 **외부 문단 1개(중첩 표)**
  안에 있어 외부 문단 경계로 못 쪼갬. 분할하려면 **중첩 표로 재귀**해 중첩셀 135문단을 페이지
  경계로 나눠야 함.

## 근본원인 방향별 정리

| # | 이슈 | 압축 위치 | 결과 | 필요 |
|---|------|----------|------|------|
| A | #2007 | 중첩셀 stored vpos(882px) < 콘텐츠(수천px) | under (통째 크램) | 콘텐츠 실측 + 중첩 재귀 분할 |
| C | #2019 | 섹션 절대 vpos(386px) < 렌더(1027px/쪽) | over (흐름 과계상) | 절대배치(흐름 0) |

→ **공통**: 페이지네이션이 stored vpos(압축)를 신뢰. **실제 콘텐츠 flow 높이**로 측정해야 함.

## Stage A2 방향 (다음)

`height_measurer.rs` 중첩셀 측정을 **stored vpos(last_seg_end) 대신 실제 문단 flow 높이 합**으로
교정(중첩 표 재귀 포함). 42065 pi=7 effective_height 882px→실제(수천px) 확인. 단, 측정만 고치면
split이 외부 문단 1개라 못 쪼갬 → Stage A3에서 중첩 재귀 분할 필요.

**위험**: height_measurer는 전 표 측정 공유 hot-path. 선언높이 권위(#1763/#501/#874) 케이스와
충돌 위험 → 압축 신호(stored vpos ≪ 콘텐츠 flow) 감지로 게이트 한정 + 광범위 표 회귀검증 필수.

## 상태
소스 미수정(계측만). 오라클 기준선: 42065 rhwp 6 vs 한글 17(현행 devel). 다음 Stage A2에서 측정
교정 시도 + 표 통합 테스트 회귀검증.
