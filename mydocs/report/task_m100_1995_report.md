# #1995 조사·부분수정 보고 — RowBreak 표 다수 문서 과소 페이지 (rhwp vs 한글 −97)

- 이슈: #1995 (M100) / 브랜치: `local/task1995`
- 일자: 2026-07-06 / 상태: **해결 — rhwp 171→270 (오라클 268, +2), 무회귀**

## 0. 결과 요약 (TL;DR)

- **근본 원인**: 제4장 임베드 매뉴얼을 전면 이미지 96장으로 삽입한 문단(pi=1004)을 rhwp가 한 페이지(p141)에 전부 스택 → 과소 페이지(rhwp 171 vs 한글 268). **RowBreak 표 문제 아님**(서베이 상관관계는 우연).
- **수정**: typeset 인라인 컨트롤 방출 루프에서, 한 문단에 본문높이 60%+ non-TAC 그림이 2장 이상이면 각 그림을 개별 페이지에 단독 배치(`typeset.rs`). + InFrontOfText treat_as_char 표 흐름 높이 예약(부수적, +3).
- **결과**: 청년주거 171→**270** (오라클 268, +2 오차). byeolpyo1/4·편람 무회귀, lib 테스트 2126 통과·0 실패.
- **잔여**: +2 미세오차(첫 이미지 force_new_page의 빈 페이지 등 추정) — 필요 시 후속 미세조정.
- **범위**: 이슈가 "심각 케이스"로 지목한 −97 대표 케이스를 해결. 나머지 minor 파일은 별개 원인(아래 §7).

## 7. 나머지 과소 파일(minor) — 별개 원인, 후속 대상

수정 후 이슈의 나머지 3개 파일 측정(변화 미미 = 다른 원인):

| 파일 | rhwp(수정후) | 한글 | Δ | 다중 전면 이미지 스택? |
|------|------:|----:|---:|---|
| 1430000 최종보고서 | 384 | 404 | −20 | 아님(전면 이미지가 문단당 1장 분산 → 이미 페이지별) |
| 1790387 HIV PrEP | 130 | 146 | −16 | 아님(한 문단 최대 controls 4) |
| 1220000 관세사 | 126 | 134 | −8 | 아님(controls 9, 전면 아님) |

이 파일들은 전면 이미지 96장 스택 패턴이 없어 본 수정 대상이 아니다. 이슈가 스스로 "Δ≤−5 6건 중 심각 케이스는 −97 단독, 그 외 minor"라 명시했으므로, 본 PR 은 심각 케이스를 해소하고 minor 3건은 각기 다른 과소 페이지 원인으로 별도 추적한다.

### 7-1. minor 3건 원인 특성화 (한글 오라클 + render-tree 대조)

각기 **다른** 원인으로 확정됐다(플래그십의 다중 전면 이미지 스택과 무관):

- **m1430000(−20)**: rhwp 이미지 페이지 160(스택 0) vs 오라클 195. 이미지+텍스트 혼재 41쪽. 오라클이 그림을 별도 페이지로 더 분리(rhwp 는 그림+본문을 같은 페이지에 채움). → **그림 배치/페이지 분리 규칙 차이**(→ 이슈 #2006 등록).
- **m1790387(−16)**: 이미지+텍스트 혼재 58쪽(이미지 91쪽). m1430000 과 같은 그림 배치 계열(→ 이슈 #2006 에 병합).
- **m1220000(−8)**: RowBreak 자리차지 표 겹침 34쪽. p4 등에서 (외부표+내부표) 쌍이 stacking 되고 세 번째 쌍이 페이지 밖(y=1066)으로 overflow. → **표 스택/행높이 계열**(#1842/#1937 축, → 이슈 #2007 등록).

세 케이스 모두 플래그십 대비 소폭(5~14%)이며 각기 delicate 한 영역(그림 배치·행높이)이라, 본 PR 범위 밖으로 두고 각각 별도 이슈로 등록하여 오라클 게이트 하에 단계적으로 다룬다.

## 1. 재현 (한글 오라클 확정)

| 대상 | 수정 전 | 수정 후 | 한글 오라클 |
|------|------:|------:|------:|
| 청년주거(−97 원본) | 171 | **174** | 268 |
| 편람(대조군, 정상) | 393 | 393 | 383(한컴 PDF) |
| byeolpyo1 / byeolpyo4 | 4 / 26 | 4 / 26 | — |

오라클: `pyhwpx PageCount` = 268. 원본은 `hwpdocs/prism_downloads/국토교통부/1613000-202200006_…청년 주거정책….hwp`.

## 2. 수정한 결함 — InFrontOfText treat_as_char 표 흐름 높이 미예약 (+3쪽)

**위치**: `src/renderer/typeset.rs:11422` (활성 `paginate()` 경로), 병렬로 `src/renderer/pagination/engine.rs:1657`.

**결함**: `글앞으로/글뒤로`(InFrontOfText/BehindText) 표를 단일컬럼에서 무조건 높이 0 `Shape` 로 방출하는 분기가 **`treat_as_char` 예외를 누락**. #703 원취지는 데코레이션(비-TAC 워터마크/배경) 표 제외인데, 인라인 콜아웃 박스(treat_as_char + 글앞으로)까지 흐름에서 빠져 후속 문단이 박스 위로 겹치고 페이지가 압축됐다. 같은 파일의 다른 InFrontOfText 지점(523·3402)은 이미 `!treat_as_char` 가드 보유 — 11422만 누락이었다.

**수정**: 단일컬럼 케이스에 `&& !table.common.treat_as_char` 가드 추가 (multicol overlay anchor 경로는 자체 TAC 판정 보유하여 무변경).

**효과/한계**:
- 페이지네이션: 박스가 `Shape`→`Table`(319px 예약)로 전환, 171→174 (+3). 회귀 없음.
- **한계**: 렌더 측(`layout.rs`)은 여전히 InFrontOfText 표를 절대위치로 그려 시각 겹침이 남는다(표-본문 겹침 22→23 불변). 이 박스류(문서 내 9개)는 페이지네이션·렌더 **양쪽** 수정이 필요하며, 규모가 작다(−97 중 +3).

## 3. 주 드라이버(−94) 최종 확정 — 전면 매뉴얼 이미지 96장이 한 페이지에 스택

수정 후에도 rhwp 174 vs 한글 268 (−94). 다음을 배제/확인:

- **순수 본문**: 한글과 완벽 정렬 (p14 문단 간격·줄 pitch 동일). 배제.
- **자리차지(TopAndBottom) 단일셀 박스**: stored line_seg 높이를 정확히 예약(예: pi=158 콘텐츠 14줄=194.7px 정확, pi 최대 673.8px). 배제.
- **over-full 페이지**: 10/174 뿐, 최대 126%. 미미.

남은 −94는 **한글이 콘텐츠를 더 많은 페이지에 분산**하는 데서 온다. 오라클 Q&A/사례 구간(예: 오라클 p186~209)은 대형 테두리 박스가 페이지당 1~2개 + 페이지 절반이 공백인데, rhwp 는 같은 콘텐츠를 더 조밀하게 담는다. 개별 박스/표 높이는 정확하므로 원인은 **페이지 분할 규칙 차이**다.

**반증된 가설 (기록)**:
- `keep_with_next`/`keep_lines` 미준수 가설 → **반증**. typeset.rs 가 이들을 미참조하는 건 사실이나, 청년주거·편람 문서 모두 해당 플래그가 **전부 0**(설정된 문단 없음). page_break_before 도 0. 페이지 분할 규칙 플래그는 원인이 아니다.

**콘텐츠 양은 동일, 밀도가 다름 (정확한 텍스트 줄 카운트)**:
- fitz 텍스트 줄: **오라클 5923 vs rhwp 6426** — rhwp 가 오히려 약간 많음. **콘텐츠 과소측정/드롭 아님**.
  - (앞서 관측한 "2.6배"는 밴드측정이 표 테두리선을 줄로 오계수한 아티팩트였음 — 정정.)
- 이미지도 정상 렌더(rhwp 128개, 최대 994px). 드롭 아님.
- **밀도 차이가 원인**: rhwp 평균 36.9줄/쪽(≈92% fill) vs 오라클 22.1줄/쪽(중앙값 9, 137/268쪽이 <12줄). 같은 콘텐츠를 오라클은 성기게(공백 많이), rhwp 는 조밀하게 담아 174 vs 268.

**★ 근본 원인 최종 확정 (결정적) — 임베드 매뉴얼 이미지 96장이 한 페이지에 스택**:

페이지별 정렬 대조로 divergence 위치를 특정했다:
- Q&A/본문 구간 정렬: rhwp p118 ≈ 오라클 p117 (동일 Q77~Q80).
- 부록(서식1~14) 정렬: rhwp p157~174 = 오라클 p251~268 (각 18쪽).
- **divergence 는 그 사이 제4장 구간**: rhwp p118→157 = 39쪽 vs 오라클 p117→251 = **134쪽**. 오라클 p120~244 중 **101쪽이 전면 이미지**.

원인: **문단 0.1004 에 전면 플로팅 이미지 96개**(각 130×182mm ≈ 전면 A4, tac=false, 제4장 "청년월세 한시 특별지원사업 매뉴얼" 본문을 이미지로 삽입)가 있는데, rhwp 는 이 96개를 **전부 한 페이지(p141)의 단일 앵커 y=−509(페이지 밖)에 완전히 스택**한다(render-tree 고유 y=1개). 한글은 각 전면 이미지를 흐름 배치하여 **~96쪽**으로 펼친다.

**정량 정합**: 96 이미지 → +95쪽 → rhwp 174 + 95 = **269 ≈ 오라클 268**. 즉 이 단일 결함이 −94 를 거의 전부 설명한다.

**#1995 진단 정정**: RowBreak 표 문제가 **아니다**. 서베이의 "RowBreak 표 다수" 상관관계는 우연(정부 연구보고서가 RowBreak 콜아웃 박스와 임베드 매뉴얼 이미지를 둘 다 많이 보유). 실제 축은 **한 문단의 다수 전면 플로팅 이미지가 페이지 흐름으로 펼쳐지지 않는 결함**.

이는 앞서 확정한 InFrontOfText treat_as_char 박스 결함(§2)과 같은 계열("플로팅 객체가 흐름 높이를 예약/전진하지 않음")의 **이미지판·다중판**이다.

## 4. 권고

- **본 커밋(부분)**: InFrontOfText treat_as_char 박스 페이지네이션 수정 유지(+3, 무회귀). 렌더 측 companion 수정은 후속.
- **주 드라이버(#1995 본체, −94) 수정**: 한 문단의 다수 전면 플로팅 이미지(문단 0.1004의 96개)를 각각 페이지 흐름으로 펼치는 페이지네이션 기능 추가. 현재 typeset 에는 "문단 내 다수 비-TAC 이미지 → 개별 페이지 분할" 로직이 없다(모두 단일 앵커에 스택). 코드 단서: `non_tac_picture_or_shape_block_height_px`(typeset.rs:592)가 MAX 로 1개 높이만 반영. 고위험(핀 테스트 1086/1156/1692 등 비-TAC 이미지 처리 delicate)이므로 samples 전수·양방향 오라클 게이트 하에 신중 구현. 착수 전 이미지 anchor(vert_rel_to/offset) 시멘틱 확정 필요.

## 6. 조사 결론 (요약)

- **해결**: 다중 전면 non-TAC 이미지 → 개별 페이지 배치(typeset.rs) 구현으로 청년주거 171→270(오라클 268). 전체 lib 테스트 2126 통과·0 실패, byeolpyo1/4·편람 무회귀. 가드(≥2 전면 non-TAC 그림/문단)로 국소 발동.
- **근본 원인**: 문단 0.1004의 전면 매뉴얼 이미지 96장이 한 앵커에 스택. #1995 는 RowBreak 아니라 다중 전면 이미지 흐름 결함(진단 정정).
- 부수 수정: InFrontOfText treat_as_char 박스(+3, 같은 계열).
- 조사 중 반증/정정: 콘텐츠 과소측정(아님, 텍스트 6426 vs 5923), keep/page_break 플래그 0, "2.6배 줄"(테두리선 아티팩트), "밀도 격차"/"stored-vs-recompute"(중간 가설) — 모두 최종 원인(이미지 스택)으로 수렴.
- 잔여 +2: 후속 미세조정 여지(첫 이미지 force_new_page 빈 페이지 등).

## 5. 산출물

- `output/poc/task1995/` : 진단 증거(대조 PNG, diagnosis_evidence.md)
- 코드: `typeset.rs`, `pagination/engine.rs` (InFrontOfText treat_as_char 가드)
